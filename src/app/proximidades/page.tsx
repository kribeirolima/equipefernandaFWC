import { Proximidades } from "@/components/proximidades/proximidades";

export const metadata = {
  title: "Locais próximos · Painel da Delegação",
};

export default function Page() {
  return <Proximidades />;
}
